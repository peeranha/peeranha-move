if [ -f .env ]; then
  export $(echo $(cat .env | sed 's/#.*//g'| xargs) | envsubst)
fi
sui client call --function giveCommunityAdminPermission --module communityLib --package $PACKAGE --args $USER_ROLES_COLLECTION $USER $USER2 $COMMUNITY --gas-budget 20000000