if [ -f .env ]; then
  export $(echo $(cat .env | sed 's/#.*//g'| xargs) | envsubst)
fi
sui client call --function grantProtocolAdminRole --module accessControl --package $PACKAGE --args $DefaultAdminCap $UserRolesCollection $USER --gas-budget 2000000000