[package]
name = "Peeranha"
version = "0.0.1"
license = "Apache-2.0"
published-at = "<CURRENT-PACKAGE-ID>"

[dependencies]
Sui = { local = "../sui/crates/sui-framework/packages/sui-framework" }

[addresses]
peeranha = "0x0"
